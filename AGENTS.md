# AGENTS.md

Development guidelines and commands for mddb project.

For specific guidelines, see:
- [Backend Guidelines](backend/AGENTS.md) - Read before editing Go code in backend/
- [Frontend Guidelines](frontend/AGENTS.md) - Read before editing App.tsx, components, or CSS

## Project Overview

mddb is a Notion-like document and database system. Frontend built with SolidJS, backend with Go. Data stored
as markdown files and images.

Run `make` for the current targets; each slow or environment-dependent target documents its cost beside its
definition.

### Documentation Changes

- **Do NOT** create a markdown file listing changes you made.
- Update `docs/PLAN.md` to reflect status changes.
- Keep `README.md` up to date.
- **Always update the purpose comment** at the beginning of a source file when the code's purpose changes. This comment is used to generate the file index in `AGENTS.md`.

### Professionalism

**Mandatory**: after making changes run `make fix`, then `make verify`. `make fix` applies every autofix and
refreshes the generated file index; `make verify` is the read-only static gate and the pre-push gate. Unit
tests are `make test`. Indentation and width come from `.editorconfig`; Ruff keeps its own copy of the width
in `pyproject.toml` because it does not read `.editorconfig`.

After making UI or API changes, also run `make test-e2e` (fast rate limits, parallel workers) or
`make test-e2e-slow` (normal rate limits, sequential). These build the binary, start a test server, and run
the Playwright tests in `e2e/`; the server log is at `data-e2e/server.log`.

### Git Hooks

`pnpm install` configures the versioned hooks in `scripts/hooks/`; run `pnpm hooks:install` (or
`make git-hooks`) to restore them after an installation that skipped lifecycle scripts. The hook scripts
document their own rules.

<!-- BEGIN FILE INDEX -->
## File Index

Autogenerated from first-line comments. Run scripts/update_agents_file_index.py to refresh.

- `.custom-gcl.yml`: golangci-lint custom binary build config: methodfilecheck module plugin.
- `.github/AGENTS.md`: GitHub Configuration Guidelines
- `.github/workflows/ci.yml`: CI workflow: build, lint, and test on push.
- `.github/workflows/pr.yml`: PR workflow: build, lint, and test on pull_request.
- `.golangci.yml`: golangci-lint configuration.
- `Makefile`: Build, verify, test, and development commands.
- `README.md`: mddb - Markdown Document & Table System
- `SELF_HOSTING.md`: Self-hosting and deployment instructions for mddb
- `backend/AGENTS.md`: Backend Development Guidelines
- `docs/ISSUES.md`: Code Review Issues
- `docs/PLAN.md`: mddb Implementation Plan
- `docs/REQUIREMENTS.md`: mddb Requirements
- `docs/TECHNICAL.md`: mddb Technical Architecture and Storage Model
- `docs/notion/IMPORT_PLAN.md`: Notion Import
- `docs/notion/README.md`: Notion Research
- `e2e/AGENTS.md`: E2E testing guidelines and best practices for Playwright tests
- `eslint.config.js`: ESLint configuration for TypeScript and SolidJS.
- `frontend/AGENTS.md`: Frontend Development Guidelines
- `playwright.config.ts`: Playwright end-to-end test configuration.
- `pnpm-workspace.yaml`: pnpm workspace configuration.
- `scripts/bench.ts`: Runs frontend benchmarks on tinybench (the engine vitest bench used) without vitest.
- `scripts/build_frontend.py`: Builds the frontend into //backend/frontend/dist/, to be embedded in the backend executable.
- `scripts/check-staged.sh`: Validates the staged snapshot with focused repository checks.
- `scripts/clean_data_e2e.py`: Clean up test data after e2e test runs.
- `scripts/install-git-hooks.sh`: Configures this checkout to run the repository's versioned Git hooks.
- `scripts/lint_binaries.py`: Lint git-tracked files for unexpected binary data and executable files.
- `scripts/lint_frontend_styles.mjs`: Lints frontend CSS tokens, modules, raw colors, and presentation embedded in TSX.
- `scripts/precompress_dist.py`: Precompress built assets so a static handler can serve them verbatim.
- `scripts/quiet-test-reporter.mjs`: Reports Node test failures and diagnostics, with compact progress only on CI.
- `scripts/run-concurrently.sh`: Runs independent commands concurrently, or sequentially in serial mode.
- `scripts/update_agents_file_index.py`: AGENTS.md index generation, summary validation, CLAUDE.md symlinks.
- `scripts/verify_e2e_data.py`: Verify e2e test data integrity after tests run.
- `sdk/API.md`: mddb API Reference
- `sdk/api.gen.ts`: Code generated by gen-api-sdk. DO NOT EDIT.
- `sdk/types.gen.ts`: Code generated by tygo. DO NOT EDIT.
- `stylelint.config.js`: Enforces consistent styles for Solid component CSS modules.
- `vite.config.ts`: Vite build configuration for the SolidJS frontend.
<!-- END FILE INDEX -->
