# GitHub Configuration Guidelines

## Style

- Minimize vertical whitespace in YAML files
- No blank lines between steps in workflows
- Use `run:` directly instead of `name:` + `run:` when the command is self-explanatory
