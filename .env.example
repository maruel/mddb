# mddb development configuration
# Copy to data/.env or run: make dev (auto-copies if missing)
#
# Note: JWT secret, SMTP config, and quotas are stored in server_config.json (auto-created).
# This file is only for OAuth credentials and bootstrap settings.

# Server settings
HTTP=:8080
LOG_LEVEL=info
BASE_URL=http://localhost

# Google OAuth (optional)
# Create at: https://console.cloud.google.com/apis/credentials
# Redirect URI: http://localhost:8080/api/auth/oauth/google/callback
#GOOGLE_CLIENT_ID=
#GOOGLE_CLIENT_SECRET=

# Microsoft OAuth (optional)
# Create at: https://portal.azure.com/
# Redirect URI: http://localhost:8080/api/auth/oauth/microsoft/callback
#MS_CLIENT_ID=
#MS_CLIENT_SECRET=
